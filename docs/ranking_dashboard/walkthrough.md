# Walkthrough - Module 5 Implementation
- **Ranking**: Bổ sung ép kiểu dữ liệu `(int)` và thực hiện kiểm tra (validate) các trường dữ liệu `score`, `rank_position`, `series_id`.
- **Dashboard**: Mở rộng các phương thức đếm trong Model như `countAll()`, viết các câu lệnh truy vấn phức tạp trong `DashboardController` và tích hợp hiển thị dữ liệu trực tiếp vào các file view `.php`.
- **Notification**: Đảm bảo sử dụng POST request khi thay đổi trạng thái đọc thông báo. Khắc phục lỗi thẻ `<a>` chứa thẻ `<form>`.
- **Security**: Đảm bảo dọn dẹp sạch sẽ mã debug trong các file controller/views.
- **Trạng thái**: Hoàn thiện 100%.